# Tap Critter

A cozy idle tap game built with Vite + vanilla JavaScript. Tap the critter, earn coins, and upgrade your tap power or idle income.

## Run locally

```bash
npm install
npm run dev
```

## Build & deploy

```bash
npm run build
```

The production build is output to `dist/`.
